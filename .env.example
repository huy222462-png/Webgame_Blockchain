# Rename this file to .env (do NOT commit your real .env to git)
# Example MongoDB connection string (replace with your own). Do NOT store real secrets in version control.
MONGO_URI=mongodb+srv://<db_user>:<password>@cluster0.example.mongodb.net/mydb
PORT=5000
